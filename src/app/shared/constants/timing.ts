/** Duration the toast notification stays visible before auto-dismiss. */
export const TOAST_DURATION_MS = 3000;

/** Duration the Konami easter-egg toast stays visible. */
export const KONAMI_FADE_MS = 4000;

/** Delay before the hover preview tooltip appears. */
export const HOVER_PREVIEW_DELAY_MS = 300;

/** Duration of the hover preview fade-out animation. */
export const HOVER_PREVIEW_FADEOUT_MS = 200;
